# Issue 3.4: Funder service — one-way ETH dispenser (off-chain HTTP)

> One-line scope: stand up the **first off-chain HTTP service in the Pangolin codebase**. A long-running daemon that (a) accepts a top-up request from a device, (b) verifies the user paid balance against D-018 `EntitlementRegistry` via a signed `Credit` attestation OR a chain-balance check (per Q-c), (c) signs and submits a `Redemption` attestation as the contract `REDEMPTION_AUTHORITY` to decrement the user on-chain balance, (d) sends ETH to the requesting device secp256k1 address from a funder-owned wallet, (e) returns a typed receipt the client crate can verify, all the while rate-limited per address, stateless beyond a small payment-ledger, and **strictly incapable of signing a revision or submitting a publish tx**. Per D-006: the funder never signs revisions, never submits transactions, never sees vault data, never holds custody of vault keys. 3.4 ALSO ships a fresh production-split-authority redeploy of the EntitlementRegistry (D-018 testnet stays as smoke-test surface; production gets D-019 — pending Q-d).
>
> **Status:** Plan-gate DRAFT 2026-05-15, awaiting Kelvin sign-off on Q-a..Q-g. Builder must not start until Kelvin lands resolutions in a "Resolved decisions" table that mirrors 2.1 / 2.2 / 2.3 / 3.1 / 3.2 / 3.3. L1..L12 + the L-section below are non-negotiables independent of how Q-a..Q-g resolve.
>
> **Security-critical: YES.** Master plan §16.3 enumerates "funder service" by name. Broader blast radius than 3.3: (a) redemption-attestation signer key handling bug = split-trust property collapses; (b) payment verification bug = treasury drain without payment; (c) rate-limiter bug = drain via valid repeated requests; (d) HTTP-surface bug (SSRF / admin endpoint) = full off-chain pwn; (e) response-signing bug = MITM tricks client into thinking ETH arrived. Same plan-gate cadence as 3.3 / 3.1 / 2.2.
>
> **Depends on:** 2.2 R-a..R-e (EIP-712 `Credit` + `Redemption` typehashes + strict-equality nonce + immutable authorities + `expiresAt`); 2.3 (D-018 deployed + verified at `0x08F8c394EB0c04ba0A4FBA1e64507b88F4b59D8d`; both authorities collapsed to pangolin-dev for smoke); 3.1 R-a..R-e (`secp256k1_signing.rs` typehash + domain primitives — funder reuses for `Redemption`); 3.2 (`derive_evm_wallet` / `EvmWallet` — but funder runs its OWN process-owned alloy `PrivateKeySigner`, NOT a `Vault`-derived wallet); 3.3 (EIP-1559 submission pattern from `chain_submit.rs` — funder reuses gas-cap / retry / receipt-mismatch defenses for `redeem` calls + ETH transfers); D-006 / D-008 / D-016. Current `main` tip `bd1e3ea`.

## Open questions for Kelvin (LOAD-BEARING — surface at TOP per §16)

### Q-a · HTTP framework

No HTTP server in the codebase. Four plausible choices:

**Option A: axum (tokio-native).** Tokio is a workspace dep (`=1.52.2`); axum 0.7.x slots on top. First-class tower-middleware for rate-limiting and timeouts; small surface; well-audited. Adds: `axum`, `tower`, `tower-http` (cors / compression / timeout / tracing layers). **Plan-gate recommends:** axum — best fit for the tokio + alloy stack.

**Option B: actix-web.** Mature, but its own runtime adapter shims tokio — runtime-mix surface. Ecosystem has moved away for new projects.

**Option C: hyper directly.** Smallest dep tree; forces hand-rolling routing + middleware + extractors. Trade: dep minimalism vs review-surface minimalism.

**Option D: tide / rocket / others.** Niche or stagnated. Not recommended.

**Kelvin call.**

### Q-b · Persistence shape

Master plan says "Stateless beyond payment ledger." Three shapes:

**Option A: Fully in-memory.** `HashMap<Address, RateLimitState>` + bounded LRU seen-set. **Pro:** zero on-disk state. **Con:** restart wipes rate-limiter (attacker triggers a restart, resets hourly counter); no audit trail.

**Option B: SQLite-backed payment ledger only.** Uses existing `rusqlite = "=0.39.0"` workspace pin (no new dep). **Pro:** durable audit trail; per-attestation replay guard survives restart; rate-limiter survives restart. **Con:** new operational surface.

**Option C: Hybrid — in-memory rate limiter + SQLite ledger.** Rate limiter in memory for performance, rebuilds on restart from ledger; ledger durable. **Plan-gate recommends:** Option C.

**Kelvin call.**

### Q-c · Payment verification protocol

Trust boundary between Kelvin billing system and the funder. Three models:

**Option A: Signed-`Credit`-attestation flow.** User pays processor off-band; processor signs a `Credit` attestation (EIP-712 per 2.2 R-a). User submits to funder along with device wallet address; funder verifies sig against `PAYMENT_AUTHORITY`, constructs + submits `redeem(...)`, on success transfers ETH. **Pro:** trust boundary cleanly enumerated; payment processor needn online at top-up time. **Con:** `expiresAt` bounds the window; the `userId → deviceAddress` mapping is the user responsibility — see L-userId-deviceAddress-binding for the defense (Q-g).

**Option B: Chain-balance check.** Funder reads `balance(userId)` from D-018 directly. **Con:** needs off-chain `userId → deviceAddress` mapping; either re-invents the Credit flow OR maintains a server-side mapping table. Does not decrement the on-chain balance unless funder ALSO submits `redeem` — back to Option A.

**Option C: Hybrid — signed credit for prod; chain-balance for testnet smoke.** Two code paths gated by `ChainEnv`.

**Plan-gate recommends:** Option A. Testnet can synthesize Credit attestations with pangolin-dev (already `PAYMENT_AUTHORITY` on D-018).

**Kelvin call.**

### Q-d · Production EntitlementRegistry redeploy strategy

D-018 has collapsed authorities. Three paths:

**Option A: Fresh redeploy with real split keys — D-019.** Two NEW Foundry keystores: `pangolin-payment-authority` + `pangolin-funder-service`. Deploy fresh `EntitlementRegistry` with both real authority addresses. Funder points at D-019; D-018 untouched.

**Option B: Keep D-018 for testnet only; defer production redeploy.** Funder tested against D-018 with collapsed authorities; production later (MVP-3+). **Con:** architectural property never validated against real key separation before mainnet.

**Option C: Hybrid — 3.4 ships split-key-capable code AND fresh TESTNET redeploy with real split keys (D-019). Mainnet deferred.**

**Plan-gate recommends:** Option C.

**Kelvin call.**

### Q-e · Rate-limiting strategy

On-chain `balance[userId]` bounds per-user financial loss; rate-limit is load-bearing for operational stability (gas burn per redeem; RPC quota). Four shapes:

**Option A: Fixed window per address.** Boundary effect: burst 2N across a boundary.

**Option B: Sliding window per address.** Eliminates boundary effect.

**Option C: Token bucket per address.** `tower::limit::RateLimitLayer` shape.

**Option D: Layered — per-address bucket AND global hourly cap.** **Plan-gate recommends:** Option D, testnet values: bucket burst 3 / refill 1-per-15-min; global cap 100/hour.

**Kelvin call** on shape + values.

### Q-f · Funder wallet management

Funder holds a secp256k1 key that signs Redemption attestations + EIP-1559 envelopes. Where?

**Option A: File-based Foundry keystore.** Same pattern as pangolin-dev. Passphrase at startup OR env var. Alloy supports via `alloy-signer-local` `keystore` feature. **Con:** rooted host = compromised.

**Option B: Env-var raw 32-byte key.** Known footgun. NOT recommended.

**Option C: HSM (KMS / dedicated).** Highest assurance; operational complexity. **Defer to mainnet.**

**Option D: Hybrid — file-based for testnet; HSM for mainnet (deferred). Signer behind `trait FunderSigner: Send + Sync` so future HSM plugs in.**

**Plan-gate recommends:** Option A for testnet (D testnet half). HSM scaffolded via trait but not implemented.

**Kelvin call.**

### Q-g · Should the Credit typehash bind the deviceAddress?

The 2.2 R-a typehash today: `Credit(bytes32 userId, uint256 amount, uint64 nonce, uint16 schemaVersion, uint64 expiresAt)` — no `deviceAddress`. An attacker with a leaked Credit attestation can submit it with their OWN device address as the ETH destination, redirecting the user paid credit.

**Option A: Add `address deviceAddress` to the Credit typehash.** Requires re-shaping 2.2 contract sketch + D-019 ships with new typehash. Permanent on-chain defense.

**Option B: Client-signed device-binding (no contract change).** Every funder request includes a separate signature from the user device wallet (3.1 signer) over `(userId, deviceAddress)`. **Cost:** extra sig per request + extra verify step. **Pro:** no contract change.

**Plan-gate recommends:** Option B for 3.4 (keep 2.2 typehash unchanged); Option A is the right longer-term answer but is a 2.2-revisit.

**Kelvin call.**

---

## Decisions locked (independent of Q-a..Q-g)

| # | Decision | Rationale |
|---|---|---|
| **L1** | The funder service **MUST NEVER** sign a revision or submit a revision-publish tx. It signs ONLY (a) `Redemption` attestations (EIP-712 typed-data; the contract REDEMPTION_AUTHORITY half of 2.2 R-a) and (b) EIP-1559 transactions submitting the `redeem(...)` call + ETH-transfer txs. D-006 verbatim: the funder never signs revisions, never submits transactions, never sees vault data, never holds custody of vault keys. | D-006 load-bearing posture. Mechanical defense: the funder crate Cargo.toml does NOT depend on `pangolin-store`, `pangolin-core`, `pangolin-crypto`, or `pangolin-ffi` — only on `pangolin-chain` (D-018/D-019 reads + tx construction) + the new `pangolin-funder-client` for shared types. CI invariant: `cargo tree -p pangolin-funder --no-default-features --edges normal` matches no vault-touching crate. |
| **L2** | The funder service is **stateless beyond payment ledger**. Master plan §4 row 3.4 verbatim. State must either (i) be reconstructible from on-chain state (`nonce[userId]`; most recent `Redeemed` events) on cold start, OR (ii) live in the payment ledger (Q-b). Restart resumes from on-chain + ledger reconstruction. | Same operational posture as v0/2.2 contracts: minimised server-side state for bounded blast radius on compromise. On-chain `nonce` + `balance` are load-bearing replay defense; off-chain ledger is belt-and-braces. |
| **L3** | The funder service is **rate-limited per address**. Resolution shape per Q-e. Rate-limit failures return HTTP 429 with `Retry-After` (RFC 9110 §10.2.3) and JSON `{"error": "rate_limited", "retry_after_seconds": N}`. The 429 body leaks NO internal counter state (only a clamped `retry_after`). | First DoS defense layer. The contract `nonce` ratchet handles ON-chain replay; rate-limiter handles OFF-chain throttling. Without it the funder wall-clock + RPC capacity are an attacker-controlled drain primitive even when ETH treasury is bounded by `balance[userId]`. |
| **L4** | The funder service is **read-only against the chain except for the redemption + ETH-transfer paths**. NO calls to `RevisionLogV1` (D-017) at all. The funder alloy `sol!` block declares ONLY `EntitlementRegistry.balance` + `EntitlementRegistry.redeem` — not the full registry ABI, and certainly not the revision-log ABI. | Honours D-006 + L1. Compile-time defense: a funder code path that calls `publishRevision` is a build error (the binding does not exist). |
| **L5** | The funder service holds REAL ETH (or testnet ETH). A compromise = loss of that balance. Defense: funder wallet balance kept at a per-operational-cycle limit (e.g., ~24 hours of expected top-ups at the rate-limiter ceiling); topped up from a cold wallet on a schedule. The cold wallet key is NOT on the funder host. Per master plan §18.5 runbook. | Hot-wallet balance is bounded operational risk; cold-wallet refill cycle is the operator job (18.5 runbook). 3.4 builds the consume mechanism; 18.5 builds the refill discipline. NO automated refill from cold wallet in 3.4 — manual refill keeps the cold key off any internet-connected host. |
| **L6** | The HTTP surface is **minimal**: at most one POST endpoint (`POST /funder/v1/top-up`) and one health endpoint (`GET /funder/v1/health` returning `{"ok": true, "commit": "...", "registry": "0x..."}` + chain id). NO admin endpoints. NO endpoints that mutate state beyond the top-up flow. NO endpoints that expose secrets. NO debug endpoints in release builds. The health endpoint is rate-limited too (lower-priority bucket). | Smallest surface that does the job. Every additional endpoint is more code to audit + more attack surface. If 18.10 (Observability) needs a metrics endpoint, that is an EXPLICIT deferred surface (Prometheus `/metrics` on a SEPARATE port bound to localhost or internal cluster network). |
| **L7** | `pangolin-store → pangolin-chain` direction preserved. Funder depends on `pangolin-chain` for D-018/D-019 reads + redemption tx submission + EIP-712 primitives; `pangolin-store` is NOT in the funder dep tree. Mirror of 3.3 L7 with L1 belt-and-braces: funder crate explicitly does NOT depend on `pangolin-store` / `pangolin-core` / `pangolin-crypto` / `pangolin-ffi`. CI invariant: `cargo tree -p pangolin-funder --no-default-features --edges normal` matches none of these. | Same anchor-types-belong-to-pangolin-chain doctrine 3.1 L8 + 3.2 L7 + 3.3 L7 locked. Plus L1 mechanical defense: funder cannot accidentally pull a vault-touching API surface into its tree. |
| **L8** | No new external crate dep WITHOUT explicit justification in Cargo.toml. Likely additions: `axum` (Q-a), `tower` / `tower-http`, `tracing` + `tracing-subscriber`, `governor` OR `tower::limit::RateLimit` (Q-e). NO new crypto crates (alloy covers secp256k1). NO `serde`-bearing crates added to the workspace non-funder crates. `serde` + `serde_json` ARE used INSIDE the funder crate for HTTP shapes (axum requires it); HIGH-1 (`cargo tree -p pangolin-crypto | grep serde == 0`) preserved because the funder is its own crate. Each addition documented in the funder Cargo.toml with a per-line rationale comment (same pattern as workspace Cargo.toml alloy block). | Same dep discipline 3.1 L7 + 3.2 L8 + 3.3 L8 locked. Funder is the first crate to NEED `serde` legitimately (HTTP request bodies); the workspace `deny.toml` serde-ban is scoped to `pangolin-crypto` tree — preserved because the funder is separate. |
| **L9** | `forbid(unsafe_code)` preserved on every NEW crate. | Mechanical. |
| **L10** | AGPL-3.0-or-later SPDX header on every NEW `.rs` file (D-016 + 1.1 onward convention). | Same L10 every plan-gate since 1.1 has locked. |
| **L11** | No on-chain broadcast of REVISION transactions by the funder. Only redemption-attestation submissions + ETH transfers. Concretely: the funder `chain_submit`-shaped helper does NOT depend on `crate::secp256k1_signing::build_signed_revision_v1` — it depends on a NEW `build_signed_redemption_v1` (3.4 ships this, shape-identical to 2.2 Credit but binding the REDEMPTION_TYPEHASH from the registry). | L1 + L4 mechanical defense restated at the signing layer. Compile-time discipline: the funder signing module imports ONLY the redemption signer + types; the revision signer is `pub(crate)` to its own module + is not re-exported into the funder scope. |
| **L12** | Funder service **does NOT log** user-identifying information (`deviceAddress`, `userId`, full Credit-attestation payload) at INFO level. DEBUG-only with sampling (configurable rate; default 1%). At INFO level: tx hash, rate-limit decision (allowed / denied), high-level error class — never the user identifier. Per master plan §18.5: "minimal logging, retention window, address-vs-payment correlation policy." | Funder is one of two services that LEARNS the userId-to-deviceAddress mapping (per 2.2 privacy posture). Logging that mapping at INFO + retaining indefinitely turns the funder into a per-user payment-history index. Privacy posture (D-005 / D-006 / §8.3) defers correlation-minimization to the operational layer; L12 makes the default safe. |

## Adversarial framing — L-section (load-bearing risks)

> Per §16, every plan-gate doc for a security-critical issue surfaces the load-bearing risks an adversary could leverage from each design choice. The funder is the broadest off-chain surface MVP-2 ships.

### L-funder-impersonation

**What goes wrong:** an attacker stands up a fake funder service at a URL similar to the real one (`funder.pangolin.example` vs `funder.pangolin.examp1e`). Users or the `pangolin-funder-client` crate are tricked into sending Credit attestations + payment details. Worst case: the fake service collects valid Credit attestations + submits them to the REAL funder later — but the on-chain `nonce` ratchet prevents that. Lesser case: the fake service denies users their ETH (DoS).

**Failure mode:** users cannot get gas top-ups; the legit funder is bypassed.

**Defense:** the funder service URL is pinned in `pangolin-funder-client` config (compile-time per release build OR runtime-configurable via signed config — `pangolin-funder-client` Q, not 3.4). The funder signs its OWN responses with its REDEMPTION_AUTHORITY secp256k1 key — a SEPARATE EIP-712 typed-data `FunderResponse` envelope over `{userId, deviceAddress, txHash, dispensedWei, timestamp}`. The client verifies that signature against the registry `REDEMPTION_AUTHORITY()` view at runtime.

**Adversary leverage:** limited to delay-or-deny — cannot reuse a Credit (nonce ratchet) and cannot forge a `FunderResponse` signature.

### L-credit-attestation-replay

**What goes wrong:** a leaked, stolen, or screen-scraped Credit attestation is submitted to the funder multiple times.

**Failure mode (mitigated):** the contract `nonce[userId]` mapping increments on every successful `redeem` (per 2.2 strict-equality — `redeem` ALSO bumps the nonce, not just `credit`); replayed Credit-then-Redeem lands as `ErrNonceTooLow` on second `redeem`. But: the funder still spent gas submitting the second `redeem` that reverted, AND may have sent ETH BEFORE confirming the receipt (depending on ordering — see L-payment-order).

**Defense:** (a) the funder tracks per-attestation seen-set in the payment ledger (Q-b) — a Credit-attestation hash already processed returns HTTP 409 without touching the chain; (b) order: redeem first, ETH-transfer second (see L-payment-order); (c) the rate-limiter (Q-e) bounds the attempt rate.

**Adversary leverage:** essentially none against the funder treasury; minor RPC capacity DoS (mitigated).

### L-funder-wallet-key-leak

**What goes wrong:** the funder REDEMPTION_AUTHORITY secp256k1 scalar leaks. Attacker can (i) sign arbitrary `Redemption` attestations against ANY userId — deflate every user balance to zero; (ii) sign ETH-transfer txs from the funder hot wallet — drain it; (iii) sign `FunderResponse` messages impersonating the funder.

**Failure mode (worst-case):** treasury drain + every user entitlement forced to zero. The contract has NO admin recovery — per 2.2 R-d, immutable authorities; fix is deploying a NEW registry + telling everyone (off-band). Days of downtime.

**Defense:**
- (a) **Key custody:** Q-f Option A on testnet; Option C (HSM) on mainnet (deferred).
- (b) **Hot-wallet balance ceiling (L5):** total compromise drains only bounded hot-wallet balance.
- (c) **Monitoring (§18.10):** dashboards for "ETH dispensed/hour" + "balance remaining"; anomalous drain fires an alert.
- (d) **Per-tx hard cap (mirror of 3.3 L6):** outbound ETH-transfer hard cap on `value` (e.g., 0.01 ETH per tx). A signed tx sending more than the cap is rejected by the funder signer wrapper BEFORE submission. Defense against a code bug + slows the drain if both happen at once.

**Adversary leverage:** treasury drain + per-user deflate are the worst losses. Recovery: deploy v2 registry (2.2 R-d) + re-key everyone. Days of downtime; bounded financial loss per L5.

### L-DOS-eth-drain (rate-limit bypass)

**What goes wrong:** a user with a legit paid balance repeatedly requests top-ups via valid Credit attestations within the rate-limit window. Each request causes the funder to (i) submit a `redeem` tx (gas burn for the funder signer), (ii) send a small ETH transfer (gas + value). The user is draining their OWN balance but each drain costs the funder operational capacity.

**Defense:**
- (a) Per-address rate-limit (Q-e).
- (b) Global rate-limit (Q-e).
- (c) Per-transfer cap (mirror of L-funder-wallet-key-leak (d)): 0.01 ETH per tx.
- (d) Per-credit-attestation-amount cap: Credit attestations with `amount > MAX_CREDIT_PER_ATTESTATION` rejected. Defense against a leaked PAYMENT_AUTHORITY key forging a 2^256-credit attestation.

**Adversary leverage:** the contract `balance` bounds per-user loss; the rate-limiter + caps bound funder operational loss per attacker.

### L-funder-service-MITM

**What goes wrong:** TLS termination is missing or misconfigured. Passive MITM observes Credit attestations + device addresses in transit (privacy leak); active MITM intercepts + injects fake responses (see L-funder-impersonation).

**Failure mode (passive):** the userId-to-deviceAddress mapping leaks to a network attacker — degrades 2.2 R-b opaque-bytes32 privacy posture.

**Defense:**
- (a) HTTPS-only. Two paths: (i) embedded TLS via `axum-server` + `rustls` (rustls is NOT on the crypto allowlist today — adding it requires Q-? deferral); (ii) behind a reverse proxy (nginx / caddy / Cloudflare) terminating TLS; funder serves plain HTTP on a localhost-bound socket. **Plan-gate recommends:** Option (ii) — operational standard for backend services; avoids putting TLS code in the Pangolin crypto tree.
- (b) HSTS + secure cookie attributes at the proxy layer (operator job, documented in 18.5 runbook).
- (c) `pangolin-funder-client` URL config rejects `http://` schemes (compile-time or runtime assertion).

**Adversary leverage:** mitigated to "operator has to put a TLS proxy in front" — documented in the runbook.

### L-payment-order (race between redeem and ETH-transfer)

**What goes wrong:** the funder flow is two sequential on-chain operations: (i) `redeem(...)` to decrement on-chain balance; (ii) ETH-transfer to device address. If (i) succeeds but (ii) fails (funder host crashes between; RPC timeout on (ii); hot wallet runs out of ETH between (i) and (ii)), the user is DEBITED on-chain but has NOT received gas. Paid for ETH they did not get.

**Defense:**
- (a) **State machine in payment ledger (Q-b):** every top-up recorded with explicit lifecycle: `received → pre_redeem → redeem_submitted → redeem_mined → eth_transfer_submitted → eth_transfer_mined → done` plus failure states. On restart, funder scans for `redeem_mined → !eth_transfer_*` rows and resumes the ETH transfer.
- (b) **Receipt-await discipline:** funder waits for `redeem` receipt BEFORE constructing ETH-transfer (3.3 R-e shape verbatim — 1-block conf). If receipt-await times out, row stays `redeem_submitted` and next restart-scan resumes (rather than retrying the redeem, which would double-debit).
- (c) **Idempotency in response:** response includes BOTH tx hashes (redeem + eth-transfer).
- (d) **Manual reconciliation path:** §18.5 runbook documents the operator process — a row stuck in `redeem_mined → eth_transfer_*` for >1h triggers an alert; operator manually completes.

**Alternative order (rejected):** ETH-first, redeem-second. **Pro:** user always gets gas. **Con:** the funder transferred ETH against a balance not decremented; if redeem then fails (unlikely under strict-equality but possible) the user got "free" ETH. Plan-gate recommends redeem-first — user-paid-for-nothing failure is recoverable via reconciliation; user-got-free-ETH failure is unbounded loss surface.

### L-replay-after-revert (mirror of 3.3 analog)

**What goes wrong:** the funder `redeem` tx mines but reverts on-chain (e.g., `ErrUnauthorizedSigner` from a Q-d misconfig; `ErrAttestationExpired` from clock skew; `ErrInsufficientBalance` from a multi-region race). The retry loop, if naive, re-submits and burns more gas.

**Defense:** mirror of 3.3 R-c retry taxonomy — contract reverts are FATAL, never retried. Retry only on (a) funder OWN-tx nonce-collision (independent of the contract userId nonce), and (b) RPC-transient. The funder `chain_submit`-shaped helper imports + uses 3.3 `ChainError` taxonomy verbatim.

**Adversary leverage:** only attacker-controllable revert path is `ErrAttestationExpired` (user delays after Credit). Funder rejects expired BEFORE submission with HTTP 400 — no gas burn. Other reverts are funder misconfig; mitigated at deploy.

### L-server-clock-skew

**What goes wrong:** funder host system clock drifts. Credit attestation `expiresAt` is compared against `block.timestamp` on chain (per 2.2 R-e) AND against `now()` on the funder side (defense-in-depth). If funder clock is faster than chain time, funder rejects valid attestations as expired. If slower, funder submits attestations chain rejects (gas burn + L-replay-after-revert).

**Defense:** (a) funder maintains a clock-skew tolerance window (e.g., ±60s) between system clock and most recent block timestamp; on cold start + every N redeems, fetch latest block timestamp + compute skew. (b) Skew exceeding tolerance logs OPERATOR-level alert (§18.10) and continues operating. (c) NTP / chrony at OS level is operator responsibility (runbook).

### L-userId-deviceAddress-binding

**What goes wrong:** funder flow receives `userId` (in the Credit, immutable) and `deviceAddress` (in the request body, mutable). An attacker submits a valid Credit under userId X with deviceAddress Y where Y is attacker-controlled. Funder dispenses ETH to Y; user X balance gets decremented. Attacker effectively redirects user X paid credit.

**Failure mode:** user X loses paid credit; attacker gets free ETH.

**How does the attacker get a valid Credit for X?** (a) X attestation leaked / stolen — nonce ratchet defeats double-spend but the FIRST use can go to any deviceAddress; (b) intercepted in transit (L-funder-service-MITM); (c) user voluntarily shared (Discord help post).

**Defense:** see Q-g. Plan-gate recommends Option B — client-signed device-binding proof. With this defense the attacker needs BOTH the leaked Credit AND a signature from the user device wallet — which requires compromising the device, at which point the attacker has bigger problems (vault unlock).

### L-secrets-in-logs

**What goes wrong:** the funder logs request bodies (full Credit attestations including secp256k1 signatures). Log files persist; aggregation services index them; backups carry them. Eventually a logfile leaks.

**Failure mode:** privacy leak (the mapping); modest replay window (if logs leak before the nonce has ratcheted past the attestation).

**Defense:** L12 verbatim — DEBUG-only with sampling; INFO never logs userId, deviceAddress, signature bytes. CI invariant: grep INFO-level macro args for the request body fields.

### L-cors-and-csrf

**What goes wrong:** funder HTTP surface allows cross-origin from arbitrary origins. A user browser at `evil.example` runs JS that triggers a request to the funder with the user cookies/auth. Funder dispenses ETH to attacker-controlled.

**Failure mode (mitigated):** the funder does not USE cookies or session auth — every request is self-contained (Credit + deviceAddress + device-binding sig). CSRF in the classical sense does not apply. BUT: a future maintainer adding cookie auth without thinking turns this into a real surface.

**Defense:** (a) CORS whitelist `Access-Control-Allow-Origin: <pangolin-client-app-only>` (not `*`); (b) `Access-Control-Allow-Credentials: false`; (c) funder NEVER sets cookies; (d) future maintainers adding auth must update this row. `tower_http::cors::CorsLayer` handles mechanics.

### L-funder-as-payment-correlator

**What goes wrong:** the funder LEARNS the `userId → deviceAddress` mapping (L-userId-deviceAddress-binding). Indefinite retention turns the funder into the per-user payment-history index the 2.2 R-b opaque-bytes32 model was supposed to prevent.

**Defense:** L12 + §18.5 "minimal logging, retention window, address-vs-payment correlation policy" — bounded retention (e.g., 30 days), no account linkage. 3.4 ships code that DEFAULTS safe; operational policy is the 18.5 job.

**Adversary leverage:** funder operator is the trust anchor; if Pangolin-the-org becomes a compelled-disclosure target, logs are subpoenaable. Defense is at the policy + retention layer.

## Affected crates

- **`services/funder/`** (new — extension of existing scaffold). **Note on path:** the master plan §17 workspace tree (line 961) places the funder server at `services/funder/`, NOT `tools/funder/`. The plan-gate task brief says `tools/funder/` but the existing scaffold lives at `services/funder/src/` (empty). **Builder MUST resolve this with Kelvin** — plan-gate recommends honoring master plan §17 (`services/funder/`). New crate (binary, NOT library — `bin/funder.rs` entrypoint):
  - `src/main.rs` / `src/server.rs` — axum routes, tower middleware stack, graceful-shutdown handler.
  - `src/handlers/top_up.rs`, `src/handlers/health.rs` — HTTP handlers.
  - `src/chain.rs` — thin alloy `sol!` block for D-019 EntitlementRegistry (ONLY `balance` view + `redeem` mutator — L4); `submit_redemption_v1(...)` helper mirroring 3.3 `publish_revision_v1`.
  - `src/signing.rs` — `Redemption` EIP-712 typed-data builder using the registry REDEMPTION_TYPEHASH + DOMAIN_SEPARATOR (read from registry on cold start + cached, OR pinned per env like 3.1 `DOMAIN_SEPARATOR_BASE_SEPOLIA_V1` constant — builder decision).
  - `src/response_signing.rs` — separate `FunderResponse` EIP-712 envelope per L-funder-impersonation.
  - `src/rate_limit.rs` — per-address + global, per Q-e.
  - `src/ledger.rs` — SQLite-backed per Q-b, using `rusqlite = "=0.39.0"` (workspace dep); schema in `src/ledger/schema.sql`.
  - `src/funder_signer.rs` — `trait FunderSigner: Send + Sync` + `AlloyKeystoreSigner` impl for Q-f Option A; future HSM impl slots in via trait.
  - `src/clock_skew.rs` — per L-server-clock-skew.
  - `src/config.rs` — clap args + env-var fallbacks; pinned defaults.
  - `src/error.rs` — `FunderError` enum + axum `IntoResponse` impl.

- **`crates/pangolin-funder-client/`** (extension of existing scaffold — today: 21-line `pub fn name()` placeholder). 3.4 extends to:
  - `pub struct TopUpRequest { credit_attestation, device_binding_signature, device_address, ... }`.
  - `pub struct TopUpResponse { redeem_tx_hash, eth_transfer_tx_hash, funder_response_signature, ... }`.
  - Thin async `TopUpClient::request_top_up(&self, ...) -> Result<TopUpResponse, FunderClientError>` that POSTs to the funder + verifies the FunderResponse signature.
  - NO HTTP server code (that is `services/funder/` job).

- **`crates/pangolin-chain`** — extend `secp256k1_signing.rs` with the EIP-712 `Redemption` typehash + `build_signed_redemption_v1(...)` helper (mirror of `build_signed_revision_v1`). **Builder Q:** redemption signer in `pangolin-chain::secp256k1_signing` (extending it) OR in `services/funder/src/signing.rs` (private)? **Plan-gate recommends:** in `pangolin-chain` — typehash is a contract-specific primitive that ALSO benefits `pangolin-funder-client` (might want to verify the funder Redemption attestation as a sanity check). Cross-crate reuse + audit-locality wins. Possibly also extend `chain_submit.rs` with a `submit_redemption_v1(...)` helper (shares the L6 gas-cap + R-c retry + R-e receipt-await with `publish_revision_v1`).

- **`crates/pangolin-store`** — NO change. Funder never touches vault data.
- **`crates/pangolin-ffi`** — NO change. Funder is a separate process.
- **`apps/cli`** — NO change in 3.4. CLI top-up trigger flow is 3.5 territory.
- **`crates/pangolin-indexer`** — NO change. 4.2 territory.
- **`contracts/`** — fresh `EntitlementRegistry` redeploy per Q-d Option C. NO source change; same `EntitlementRegistry.sol`; new deploy via existing `contracts/script/DeployEntitlementRegistry.s.sol` + two new authority addresses. `contracts/deployments/base-sepolia.json` gains a new top-level entry; `DECISIONS.md` gains a D-019 entry.
- **`Cargo.toml` (workspace)** — `[workspace] members` gains `"services/funder"`.

## Schema migration

**None required for any user-facing `.pvf` vault file.** Funder state is its own SQLite database (`./funder-ledger.sqlite` per Q-b default); migrations are funder-internal. Initial schema (per Q-b Option C):
- `payment_ledger(id INTEGER PK, attestation_hash BLOB UNIQUE, user_id BLOB, device_address BLOB, amount_credits INTEGER, redeem_tx_hash BLOB, eth_transfer_tx_hash BLOB, status TEXT, created_at INTEGER, updated_at INTEGER)`.
- `rate_limits(device_address BLOB PK, token_count INTEGER, last_refill_unix INTEGER)`.
- `schema_version(version INTEGER PK, applied_at INTEGER)`.

The funder schema-versioning follows §18.7 same as `pangolin-store` `.pvf` versioning — a future v2 schema requires a migration script, and an unknown future version reads as "operator must upgrade the funder binary".

## Test plan

The funder is the first off-chain HTTP service; the test surface is structurally novel. Mirror of 3.3 discipline:

| Test | Criterion | Location |
|---|---|---|
| `top_up_happy_path_hermetic` — POST a valid Credit + device-binding sig; funder verifies + submits redeem (MockProvider) + sends ETH (same mock); 200 OK with both tx hashes + a verifiable FunderResponse sig | end-to-end hermetic | `services/funder/tests/top_up_hermetic.rs` |
| `top_up_rejects_expired_attestation` — Credit with `expiresAt < now()`; funder returns 400 BEFORE chain call | L-server-clock-skew + Q-c | same |
| `top_up_rejects_invalid_payment_signature` — corrupt signature; funder returns 400; no chain call | L1 + L-credit-attestation-replay | same |
| `top_up_rejects_wrong_payment_authority` — Credit signed by a wallet OTHER than the registry PAYMENT_AUTHORITY; funder returns 400 after verification | L-credit-attestation-replay | same |
| `top_up_rejects_missing_device_binding_signature` — per Q-g Option B, every request includes a separate device-signed proof of `(userId, deviceAddress)`; missing or invalid proof → 400 | L-userId-deviceAddress-binding | same |
| `top_up_rejects_attestation_replay` — submit same Credit twice; first succeeds; second returns HTTP 409 (off-chain ledger seen-set) without touching chain | L-credit-attestation-replay | same |
| `top_up_rate_limit_per_address_429` — N+1 requests from same device address; (N+1)th returns 429 + Retry-After | L3 + Q-e | same |
| `top_up_rate_limit_global_429` — G+1 requests across many addresses; (G+1)th returns 429 | L3 + Q-e | same |
| `top_up_redeem_revert_is_fatal` — MockProvider returns `ErrUnauthorizedSigner` revert on redeem; funder returns 500 (NOT 4xx — funder misconfig, not user error); ledger marks `failed_redeem_reverted`; NO ETH transfer | L-replay-after-revert + L-payment-order | same |
| `top_up_eth_transfer_failure_recoverable` — Mock succeeds on redeem, fails on ETH transfer (RPC timeout); ledger stays in `redeem_mined → eth_transfer_failed`; restart-scan resumes; idempotency holds (NO double redeem) | L-payment-order | same |
| `funder_response_signature_verifies` — response FunderResponseSignature verifiable against registry `REDEMPTION_AUTHORITY()`; tamper one byte → verification fails | L-funder-impersonation | `crates/pangolin-funder-client/tests/response_verify.rs` |
| `funder_response_signature_recovers_wrong_address_on_tamper` — adversarial: flip any byte of the response; verify recovers a different address; client rejects | L-funder-impersonation | same |
| `eth_transfer_value_cap_rejects_overlarge` — outbound cap (0.01 ETH/tx default); a credit-amount mapping to > 0.01 ETH is REJECTED at the funder layer with 400 before chain call | L-DOS-eth-drain + L-funder-wallet-key-leak (d) | `services/funder/tests/transfer_cap.rs` |
| `credit_amount_cap_rejects_overlarge` — Credit with `amount > MAX_CREDIT_PER_ATTESTATION` rejected before signature verification | L-DOS-eth-drain (d) | same |
| `clock_skew_alert_fires` — mock provider whose latest-block timestamp is 90s ahead of funder system clock; funder logs operator alert + continues serving | L-server-clock-skew | same |
| `health_endpoint_returns_funder_version_and_registry_address` — GET /funder/v1/health returns commit-hash + registry address + chain id | L6 | same |
| `health_endpoint_is_rate_limited` — N+1 health requests from same IP → 429 | L3 + L6 | same |
| `no_admin_endpoints_exist` — POST /funder/v1/admin → 404; GET /metrics on the public port → 404 | L6 | same |
| `cors_rejects_unknown_origin` — Origin: https://evil.example → CORS rejection | L-cors-and-csrf | same |
| `secrets_not_in_info_logs` — invoke a top-up under tracing-test capture; grep INFO log lines for credit sig bytes / userId / deviceAddress → 0 hits; DEBUG line includes them (verifies discrimination) | L12 + L-secrets-in-logs | same |
| `funder_does_not_submit_revisions` — funder alloy `sol!` block contains ONLY `EntitlementRegistry.balance` + `redeem`; no `RevisionLogV1`-binding declared; mechanical compile-time test (a `compile_fail` doctest that tries to construct a `publishRevision` calldata and fails to compile) | L1 + L11 | `services/funder/tests/no_revision_path.rs` |
| `funder_crate_has_no_pangolin_store_dep` — CI invariant via `cargo tree -p pangolin-funder` matching no vault-touching crate | L1 + L7 | `.github/workflows/ci.yml` invariants job |
| `forbid(unsafe_code)` survives on every NEW crate | L9 | mechanical |
| `agpl_spdx_on_every_new_rs_file` — CI grep across `services/funder/src/**/*.rs` for the SPDX header | L10 | CI |
| Hermetic CI invariants — existing checks preserved | L7 + L9 | CI invariants job |
| **Network-gated** (`--features integration-tests`, `#[ignore]`'d, manual only): `live_d019_split_authority_redeem_smoke` — point funder at freshly-deployed D-019; pangolin-dev synthesizes a Credit (acting as PAYMENT_AUTHORITY); funder signs a Redemption (acting as REDEMPTION_AUTHORITY); both txs land on chain; receipts decoded; balance + nonce advanced. **Per env-quirk #14: this is the only test that catches contract-side semantics drift in the redeem flow** — hermetic tests with MockProvider would miss exactly the kind of bug 3.3 `encPayload` regression was. | env-quirk #14 + Q-d + Q-f | `services/funder/tests/integration_live.rs` |
| **Network-gated**: `live_d019_attestation_replay_rejected_by_contract` — submit same redemption twice; second lands as `ErrNonceTooLow` on chain | L-credit-attestation-replay | same |

**CI test-count baseline:** ~25 hermetic + 2 live + invariants. Builder fills actual count at SIGNOFF.

## CI / build-gate impacts

- **env-quirk #14 is the LOAD-BEARING test-plan concern.** Hermetic tests with `MockProvider` cannot catch contract-side semantics for the `redeem` flow — same shape that bit 3.3 `encPayload` regression. **The `#[ignore]`'d `live_d019_split_authority_redeem_smoke` test is the cheapest manual-pre-merge sanity check.** Builder runs locally before merge; CI does not. Same posture 3.1 R-e + 3.3 R-f locked.

- **New deps under L8** require justification:
  - `axum` (+ `axum-server` if embedded TLS — likely not, per L-funder-service-MITM).
  - `tower` + `tower-http` — middleware (rate-limit, timeout, cors, tracing).
  - `tracing` + `tracing-subscriber` — structured logging. Workspace gap today; legitimate need.
  - `governor` OR `tower::limit::RateLimit` — Q-e. `governor` is more featureful; `tower::limit::RateLimit` is simpler.
  - `serde` + `serde_json` — HTTP shapes. **First time `serde`-derive lands in any Pangolin crate.** Per L8: funder is a separate binary crate; HIGH-1 (`cargo tree -p pangolin-crypto | grep serde == 0`) is preserved.
  - `chrono` — likely not needed (use `core::time::Duration` + unix timestamps).

- **`cargo deny check`:** funder crate deps evaluated against `deny.toml`. The serde-ban is scoped to `pangolin-crypto` tree; funder is exempt by virtue of being separate.

- **`cargo audit`:** existing ignores preserved; new ones if axum transitive surfaces known-acceptable RUSTSECs.

- **`cargo tree -p pangolin-funder --depth 1`:** documented in funder README as the canonical dep-surface to review pre-merge.

## Threat-model touch points

3.4 fills in `THREAT_MODEL.md` per-component table row currently `TBD (issue 3.4 plan)`: **"Funder service"** — promoted from TBD to DOCUMENTED (issue 3.4). Per-surface enumeration is the L-section above. Numbered invariant additions: NONE NEW (invariants #1..#12 cover funder surface — #2 in particular, "Relay/funder/indexer cannot forge revisions," is reaffirmed mechanically by L1 + L4 + L11 + L-funder-wallet-key-leak analysis).

## Files expected to change

- `services/funder/Cargo.toml` (new) — binary crate manifest; per-dep rationale comments per L8.
- `services/funder/src/main.rs` (new) — entrypoint: arg parsing, signer load, server start, graceful shutdown.
- `services/funder/src/server.rs` (new) — axum router + tower middleware stack.
- `services/funder/src/handlers/top_up.rs` (new) — POST /funder/v1/top-up.
- `services/funder/src/handlers/health.rs` (new) — GET /funder/v1/health.
- `services/funder/src/chain.rs` (new) — alloy `sol!` for D-019 `EntitlementRegistry` (balance + redeem ONLY); `submit_redemption_v1(...)` helper.
- `services/funder/src/signing.rs` (new) — `Redemption` typed-data builder + signer.
- `services/funder/src/response_signing.rs` (new) — `FunderResponse` typed-data builder + signer.
- `services/funder/src/rate_limit.rs` (new) — per-address + global rate-limit.
- `services/funder/src/ledger.rs` (new) — SQLite payment-ledger interface.
- `services/funder/src/ledger/schema.sql` (new) — initial schema + future migrations.
- `services/funder/src/funder_signer.rs` (new) — `trait FunderSigner` + `AlloyKeystoreSigner` impl.
- `services/funder/src/clock_skew.rs` (new) — clock-skew monitor.
- `services/funder/src/config.rs` (new) — clap-derived config + env-var fallbacks.
- `services/funder/src/error.rs` (new) — `FunderError` enum + `IntoResponse`.
- `services/funder/tests/top_up_hermetic.rs` (new) — hermetic suite.
- `services/funder/tests/integration_live.rs` (new) — `#[ignore]`'d live tests.
- `services/funder/tests/no_revision_path.rs` (new) — mechanical L1+L11 compile-time check.
- `services/funder/tests/transfer_cap.rs` (new) — outbound cap tests.
- `services/funder/README.md` (new) — operator-facing readme.
- `docs/operations/funder-runbook.md` (new) — §18.5 deliverable.
- `crates/pangolin-funder-client/Cargo.toml` — extend with HTTP-client deps (likely `reqwest` or a thin hyper wrapper; possibly shared types via this crate — builder decision).
- `crates/pangolin-funder-client/src/lib.rs` — extend from placeholder.
- `crates/pangolin-funder-client/src/types.rs` (new) — request/response types.
- `crates/pangolin-funder-client/tests/response_verify.rs` (new) — FunderResponse verification tests.
- `crates/pangolin-chain/src/secp256k1_signing.rs` — extend with `Redemption` typehash + `build_signed_redemption_v1`.
- `crates/pangolin-chain/src/chain_submit.rs` — possibly extend with `submit_redemption_v1(...)` helper.
- `crates/pangolin-chain/src/lib.rs` — re-export new symbols.
- `Cargo.toml` (workspace root) — add `"services/funder"` to `[workspace] members`.
- `contracts/script/DeployEntitlementRegistry.s.sol` — NO source change; re-run for D-019 redeploy.
- `contracts/deployments/base-sepolia.json` — new top-level `EntitlementRegistryProd` (or similar; name pending Q-d) entry.
- `DECISIONS.md` — append D-019 entry (post-deploy) + R-rows recording Q-a..Q-g resolutions.
- `THREAT_MODEL.md` — promote `Funder service` row from TBD to DOCUMENTED; add L-section per-surface entries.
- `docs/architecture/funder-service.md` (new) — operator-facing architecture summary.
- `DEVLOG.md` — append entry at merge.
- `.github/workflows/ci.yml` — add `cargo tree -p pangolin-funder` invariant; add funder crate to test-job matrix.
- `deny.toml` — per-crate exemption comment if needed (serde-ban-pangolin-crypto preserved).

Files NOT expected to change:
- Any `crates/pangolin-store/*`, `crates/pangolin-core/*`, `crates/pangolin-crypto/*`, `crates/pangolin-ffi/*` — per L1.
- `crates/pangolin-indexer/*` — 4.2 territory.
- `apps/cli/*` — 3.5 territory.
- `contracts/src/*` — no Solidity changes.

## Out of scope (explicit)

- **Production HSM signer for funder wallet.** Q-f Option C. Deferred to MVP-3 mainnet.
- **Payment-processor integration (Stripe / Apple / Google billing).** Master plan L97. 3.4 trusts a PAYMENT_AUTHORITY-signed Credit attestation; minting is billing-service job.
- **Funder UI / status page.** §18.10 + §18.5 runbook.
- **CLI subcommands** (`pangolin top-up` etc.). 3.5 territory.
- **Metering / analytics for retention or pricing.** Privacy-incompatible with 2.2 R-b opaque-userId posture if done naively.
- **Per-user fraud detection / KYC-style checks.** Off-chain billing job; 3.4 trusts the PAYMENT_AUTHORITY.
- **Multi-region funder deployment / horizontal scaling.** §18.5 scaffolding territory; 3.4 ships a single-instance binary.
- **Automated cold-wallet → hot-wallet refill.** L5 rationale — manual refill keeps the cold key off the funder host. §18.5 describes the manual cycle.
- **Mainnet deployment.** D-019 is testnet only. Mainnet redeploy + funder bring-up gated on D-011 (external audit, deferred to before MVP-3 mainnet) + Kelvin authorization.
- **Cross-chain support.** Master plan §0 cardinal principle 4: one chain per build. Funder hardcodes Base Sepolia via `ChainEnv::BaseSepolia`.
- **Funder-to-funder failover / standby instances.** Single-instance is an accepted availability boundary for MVP-2 testnet.
- **`pangolin-funder-client` runtime-configurable URL config.** That is a `pangolin-funder-client` Q; 3.4 ships the client with a compile-time pinned URL (overridable via env var for dev/test).
- **Browser-extension wallet integration.** MVP-4.

## Estimated effort

~24-36 hours, broken down roughly:

- `services/funder/src/main.rs` + `server.rs` + `handlers/*` (axum routes, middleware, top-up flow orchestration) — ~6-8 h.
- `services/funder/src/chain.rs` + `signing.rs` + `response_signing.rs` (alloy bindings, redemption signer, FunderResponse signer) — ~4-6 h.
- `services/funder/src/ledger.rs` + schema + state-machine for the redeem → eth-transfer flow — ~4-5 h.
- `services/funder/src/rate_limit.rs` + clock-skew monitor + config + error taxonomy — ~3-4 h.
- `services/funder/src/funder_signer.rs` (trait + AlloyKeystoreSigner impl) — ~1-2 h.
- `crates/pangolin-funder-client/*` extension (request/response types + HTTP client + response verification) — ~3-4 h.
- `crates/pangolin-chain/src/secp256k1_signing.rs` extension (Redemption typehash + helper) — ~1 h.
- Hermetic tests (top_up_hermetic.rs + transfer_cap.rs + no_revision_path.rs + cors/secrets/health tests) — ~3-4 h.
- `#[ignore]`'d live tests (live_d019_redeem_smoke + replay) — ~1-2 h.
- D-019 redeploy via `DeployEntitlementRegistry.s.sol` + record in `base-sepolia.json` — ~1 h.
- Documentation (THREAT_MODEL row + per-surface entries; funder-service.md; funder-runbook.md; DECISIONS R-rows + D-019; DEVLOG; README) — ~3-4 h.

If Q-d resolves to Option B (no redeploy; D-018 only): subtract ~1-2 h for redeploy + threat-model rework.
If Q-b resolves to Option A (in-memory only, no SQLite): subtract ~3-4 h for ledger code; but the L-payment-order defense gets WEAKER (no durable state for restart-recovery) — user-paid-for-nothing race becomes user-visible more often.
If Q-c resolves to Option C (hybrid testnet/prod): add ~2-3 h for two-path code structure.
If Q-f resolves to Option D (HSM scaffolding even for testnet): add ~3-5 h to plumb the trait + stub HSM impl.
If Q-g resolves to Option A (contract change adding deviceAddress to Credit typehash): add ~3-5 h for 2.2 revisit + new D-019 typehash + threat-model update.

---

## Builder notes (advisory; not load-bearing)

1. **The location of the funder server.** The plan-gate task spec says `tools/funder/`; the master plan §17 workspace tree (line 961) and the existing scaffold say `services/funder/`. **Builder MUST resolve this with Kelvin before starting.** Plan-gate recommends honoring §17 (`services/funder/`). The empty scaffold dir at `services/funder/src/` confirms the intended location.

2. **Existing scaffolds:** `crates/pangolin-funder-client/` is a real workspace member (Cargo.toml present, 21-line lib.rs placeholder); `services/funder/src/` is an EMPTY directory with no Cargo.toml — it is NOT in the workspace members list. Builder ADDS it.

3. **D-006 verbatim** (locked): "The funder never signs revisions, never submits transactions, never sees vault data, never holds custody of vault keys." Three of these are L1 / L4 / L11. The fourth ("never submits transactions") is technically VIOLATED — the funder submits `redeem(...)` + ETH-transfer txs. D-006 wording refers specifically to USER transactions (revisions); the funder DOES submit its own. Worth a doc clarification in `DECISIONS.md`. Plan-gate reads it as "the funder never submits transactions on behalf of users for revision publishing" — consistent with D-006 later sentence "Pangolin operates a one-way funder service that, on confirmed payment from the user, sends ETH" which IS a transaction the funder submits.

4. **The contract `Credit` typehash does NOT bind the deviceAddress** (see L-userId-deviceAddress-binding + Q-g). Plan-gate recommends client-signed device-binding as the 3.4 defense (no contract change). If Kelvin disagrees and wants the typehash extended, that becomes a 2.2-revisit + D-019-shape change.

5. **The funder outbound TX surface IS distinct from 3.3 revision-publish surface** even though both submit EIP-1559 txs via alloy. Plan-gate recommends extending `pangolin-chain::chain_submit` with a `submit_redemption_v1(...)` helper that shares the L6 gas-cap + R-c retry taxonomy + R-e receipt-await discipline with `publish_revision_v1`. The shared discipline is load-bearing; duplicating inside `services/funder/src/chain.rs` would invite drift.

6. **Per env-quirk #14:** contract-side semantics drift (the `encPayload` class of bug from 3.3) is invisible to hermetic tests. The `#[ignore]`'d live integration test against D-019 is the only catch-all. Builder runs it locally before requesting merge. Kelvin sees this output in the merge-readiness surface.

7. **Q-g (added during plan-gate drafting):** should the Credit typehash bind the deviceAddress? See L-userId-deviceAddress-binding for the trade-off.

---

## Test-count + bytecode baseline placeholder

Filled in post-implementation:

- `cargo test -p pangolin-funder` count: TBD
- `cargo test -p pangolin-funder-client` count: TBD
- `#[ignore]`'d live test count: 2
- `cargo tree -p pangolin-funder --depth 1` dep count: TBD (estimate ~12-15 direct deps)
- `services/funder` runtime binary size (release, stripped): TBD (estimate ~8-12 MB given alloy + tokio + axum)
- D-019 deploy gas: TBD (estimate ~600,000 — same shape as D-018; same source, same constructor)
- D-019 address: TBD (post-redeploy)