{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — the explicit allow-list of Tauri commands the Pangolin desktop shell exposes to its frontend. Mirrors apps/desktop/src/lib.rs's invoke_handler registry; principle-of-least-authority per the MVP-4-B plan §3.5 (L4).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-write-text"
  ]
}
